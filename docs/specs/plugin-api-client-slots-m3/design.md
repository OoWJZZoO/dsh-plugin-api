# Stage 2 Design: plugin-api-client-slots-m3

C4 is a frozen forwarding shell around `register`, `inject`, `entries`, and `subscribe`. A callback supplied by integration reports completed mutations to C5.
