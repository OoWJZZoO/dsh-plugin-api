# Stage 2 Design: plugin-api-client-settings-remote-m3

ST5 receives the C2 owner, C9/ST6-ready descriptor contribution, and client remote map from W5. It never calls `$mount` itself or owns codec construction.
