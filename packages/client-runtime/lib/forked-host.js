/**
 * Vendored fork of the official `@deepseek-ai/dsh-client-runtime` host
 * entry (`lib/index.js`, version 0.1.0-rc.6), MIT licensed.
 *
 * Source: /usr/lib/node_modules/@deepseek-ai/dsh/node_modules/@deepseek-ai/dsh-client-runtime/lib/index.js
 * Forked verbatim. The official import surface is never covered:
 * `import "@deepseek-ai/dsh-client-runtime"` still resolves the official package.
 */
//#region lib/types/index.js
/** Host loader entry for the browser runtime exported from `./client` and `./loader`. */
/** Host plugin body — no host-side behavior for the runtime plugin. */
function apply(_ctx) {}
//#endregion
export { apply };
