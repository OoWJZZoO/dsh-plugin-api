// This companion deliberately does not claim the official
// @deepseek-ai/dsh-attachment-local invariant identity.  The replacement
// owns only the loader row and its ctx service face.
export const name = 'plugin-api-attachments-invariant'
export const inject = []
export function apply() {}
