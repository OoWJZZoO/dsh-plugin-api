# dsh bundle patch for @deepseek-ai/dsh-plugin-api-compaction-events.
# R3 boundary: we replace the `ctx.compaction` service/event face ONLY; the
# optional `@deepseek-ai/dsh-compaction-basic` import face stays official
# (see requirements 3.6 and docs/capability-strategy.md R3).
- id: compaction-basic
  disabled: true
- insert:
    - id: compaction-events-r1
      name: '@deepseek-ai/dsh-plugin-api-compaction-events'
      config: {}
